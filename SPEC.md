# 仕様書 — Equal-Area 図法 解説シングルページサイト

- 文書バージョン: 1.0
- 作成日: 2026-09-09
- 対象: 静的シングルページ Web サイト（Vercel でホスティング予定）

---

## 1. 目的

正積図法（Equal-Area Projection）とは何かを、専門知識のない読者が数分で理解できる形で提示する。
あわせて、2026 年 9 月に国連総会で採択された決議によって正積図法が注目された経緯を、
評価を加えず事実のみを並べた年表として示す。

読者像は「35 歳の小学生」= 一般常識と読解力はあるが、地図学・数学の専門教育を受けていない成人。

---

## 2. 正積図法の定義（調査結果）

### 2.1 言葉の定義

正積図法（equal-area projection / equivalent projection / authalic projection）とは、
**地図上のあらゆる領域について、面積の比が地球上の対応する領域の面積の比と一致する図法**である。

地図上の任意の領域 A, B と、それに対応する地球上の領域 A', B' について
`area(A) / area(B) = area(A') / area(B')` が常に成り立つ。

面積の保存は全域で成り立つか、どこでも成り立たないかのいずれかであり、
「一部の地域だけ正積」という図法は存在しない。

### 2.2 数学的定義

半径 R の球面上の点を緯度 φ、経度 λ で表し、平面座標を x(φ, λ), y(φ, λ) とする。
この写像が正積であるとは、写像のヤコビ行列式が次を全域で満たすことをいう。

```
(∂x/∂λ)(∂y/∂φ) − (∂x/∂φ)(∂y/∂λ) = s · cos φ        ( s は定数 )
```

球面の面積要素が `dA = R² cos φ dφ dλ` であることから、
上式は「地図上の面積要素が地球上の面積要素の定数倍である」ことと同値である。

回転楕円体（離心率 e）の場合は右辺が次に置き換わる。

```
s · cos φ · (1 − e²) / (1 − e² sin²φ)²
```

### 2.3 ティソーの指示楕円による特徴づけ

各点における主尺度因子を a, b とすると、正積図法の条件は

```
a · b = 1   （全域で一定）
```

である。すなわち指示楕円の面積は全域で等しく、形（扁平率 a/b）だけが場所ごとに変わる。
正角図法では逆に `a = b`（円のまま、大きさが変わる）となる。

### 2.4 正積と正角は両立しない

ガウスの驚異の定理（Theorema Egregium, 1827）により、ガウス曲率は等長写像で不変である。
球面（曲率 1/R² ≠ 0）と平面（曲率 0）は等長にできない。
正積かつ正角である写像は局所的に等長写像となるため、球面から平面への写像では存在しえない。
したがって **正積図法は必ず形をゆがめる**。これは欠陥ではなく数学的な帰結である。

### 2.5 本サイトで描画する図法: Equal Earth

Equal Earth 図法（2018 年、Bojan Šavrič・Bernhard Jenny・Tom Patterson）を採用する。
擬円筒図法であり、Putniņš P4′ 図法と Eckert IV 図法の組み合わせを基礎に、
見た目の自然さを目視で調整して係数が決められている。

単位球・中央経線 λ₀ の場合の投影式は以下。

```
sin θ = (√3 / 2) · sin φ

x = 2√3 (λ − λ₀) cos θ / ( 3 · ( 9A₄θ⁸ + 7A₃θ⁶ + 3A₂θ² + A₁ ) )
y = A₄θ⁹ + A₃θ⁷ + A₂θ³ + A₁θ

A₁ =  1.340264
A₂ = −0.081106
A₃ =  0.000893
A₄ =  0.003796
```

実装では上式を自前で書かず、同式を実装した d3-geo の `d3.geoEqualEarth()` を用いる。

---

## 3. 画面構成（縦一列・シングルページ）

| # | セクション | 内容 |
|---|-----------|------|
| 1 | ヒーロー（ファーストビュー） | Equal Earth 図法の世界地図。既定では横ドラッグで経度方向のみ移動。「緯度方向を固定」を外すと上下にも移動。中央経線・中央緯線の値を常に小数第 1 位まで表示 |
| 2 | 正積図法とは | 平易な定義。面積が正しい／形はゆがむ |
| 3 | なぜ形がゆがむのか | 球は平面に開けない、という話。みかんの皮の比喩 |
| 4 | 図法が生まれた経緯 | Lambert (1772) から Equal Earth (2018) まで |
| 5 | 2026 年 9 月に話題になった理由 | 国連総会決議 A/80/L.104 の事実関係 |
| 6 | 年表 | 古い順に上から縦に並べる。取り上げる図法はメルカトル図法・ゴール＝ペータース図法・Equal Earth 図法に絞り、ガウスの定理と政治的な経緯を加える |
| 7 | 出典 | 注釈番号つきの出典一覧 |

---

## 4. 地図インタラクション仕様

- 表示幅: 本文の内容幅（720 − 24×2 = 672px）の 1.5 倍にあたる 1008px。地図だけを幅広の
  コンテナ（`.wrap--wide`, max-width 1056px）に置き、本文は 720px のまま保つ
- 図法: `d3.geoEqualEarth()`
- 描画: `<canvas>`（devicePixelRatio 対応）。ドラッグ中の再描画コストを抑えるため SVG ではなく Canvas を使う
- **既定は経度方向のみ移動**: `projection.rotate([λ, φ, 0])` のうち λ を変更する。傾き（3 番目）は常に 0
- **「緯度方向を固定」チェックボックス**（既定でオン）を外すと φ も変更できるようになる。
  φ は ±90° に収める（超えると南北が反転するため）。固定に戻したときは φ を 0 に戻す
- ドラッグ量 → 角度の変換: 地図幅 W に 360°、地図高さ H に 180° を対応させ、
  `Δλ = −Δx · 360 / W`、`Δφ = −Δy · 180 / H`
- λ は `((λ + 180) mod 360) − 180` で −180〜180 に正規化して表示する
- 入力: マウス（pointer events）、タッチ、キーボード（地図に `tabindex="0"`、
  矢印で 5°、Shift + 矢印で 30°、Home で 0°）。上下矢印は固定を外している間だけ効く
- `touch-action` は固定中が `pan-y`（縦スクロールを妨げない）、固定を外すと `none`（縦も地図が受け取る）
- 描画要素: 球の外形（sphere）、経緯線網（10° 間隔）、陸地・国境、中央経線の目印。
  固定を外している間は中央緯線の目印も描く
- 読み取り表示: 中央経線・中央緯線とも常に表示し、常に小数第 1 位まで書く（0 のときも `0.0°`）
- 静止時のみアンチエイリアス品質を優先し、ドラッグ中は経緯線の間引きなどは行わない（110m データなら十分軽い）
- リサイズ時は ResizeObserver で再計算する
- JavaScript が無効な場合は、地図領域に代替テキストを表示する

---

## 5. 年表の実装方針（後から追記可能）

年表は HTML に直接書かず、**JSON データを読み込んで描画する**。

`public/data/timeline.json`:

```json
{
  "entries": [
    {
      "id": "1772-lambert",
      "date": "1772",
      "sort": "1772-01-01",
      "title": "ランベルトが正積図法を数学的に定式化",
      "body": "淡々とした事実の記述。",
      "sources": ["wp-lambert-cyl"],
      "figure": {
        "src": "./assets/img/proj-mercator.svg",
        "alt": "図の説明"
      }
    }
  ]
}
```

| フィールド | 必須 | 内容 |
|-----------|------|------|
| `id` | ✓ | 一意な識別子（アンカーに使う） |
| `date` | ✓ | 画面に出す日付表記（"1772" / "2026-09-04" など自由文字列） |
| `sort` | ✓ | 並べ替え専用キー（ISO 8601 の日付。日が不明なら 01 で埋める） |
| `title` | ✓ | 見出し（1 行） |
| `body` | ✓ | 本文。起きたことだけを書く |
| `sources` | | `sources.json` の `id` の配列。注釈番号が自動で付く |
| `figure` | | `{ src, alt }`。書くと詳細文の左側に画像が並ぶ |

**追記手順**: `entries` 配列に 1 件追加するだけ。並び順は `sort` の昇順でスクリプトが自動整列するため、
配列内の位置は問わない。ビルド作業は不要。

**図の方針**: `figure` は図法の外形を示す項にだけ付ける。政治的な経緯を書いた項には付けない。
外形図は `node scripts/build-thumbs.mjs` が同梱の d3-geo と Natural Earth のデータから
SVG を生成する（`public/assets/img/`）。図法を足すときはこのスクリプトに 1 行足して再生成する。

**記述方針（公平性）**:
- 起きた事実、日付、主体、数値のみを書く
- 「画期的」「ようやく」等の評価語を使わない
- 賛成・反対の双方が存在する事柄は、双方の事実を同じ粒度で書く
- 決議・声明は「何を求めたか」と「何を求めていないか」を併記する

---

## 6. 出典の実装方針

`public/data/sources.json` に出典を配列で持つ。

```json
{
  "sources": [
    {
      "id": "un-news-2026",
      "title": "UN tells the world: Stop making Africa look small",
      "publisher": "UN News",
      "url": "https://news.un.org/en/story/2026/09/1168284",
      "accessed": "2026-09-09"
    }
  ]
}
```

- 注釈番号は `sources.json` の配列順に 1 から自動採番する
- 本文中の使用箇所には `<a class="fn" data-src="un-news-2026"></a>` を置き、スクリプトが `[n]` を描画する
- 番号をクリックするとページ末尾の該当出典へジャンプする
- 出典一覧はページ最末尾に連ねて表示する
- 出典を追加するときは `sources.json` に 1 件足し、本文に `data-src` を置くだけ

---

## 7. 使用データ・ライブラリ

| 名称 | 用途 | ライセンス |
|------|------|-----------|
| Natural Earth 1:110m Cultural Vectors | 国境・陸地形状 | パブリックドメイン |
| world-atlas `countries-110m.json` | 上記を TopoJSON 化したもの | ISC |
| d3-geo 3.1.1 / d3-array 3.2.4 | Equal Earth 投影・描画 | ISC |
| topojson-client 3.1.0 | TopoJSON → GeoJSON 変換 | ISC |

すべてローカルに同梱し、外部 CDN へのリクエストは行わない。

---

## 8. デザインシステム

無印良品ネットストア デザインシステム（awesome-design-md-jp）に準拠する。

- 背景 `#fff` / ヒーロー背景 きなり `#f4eede` / アクセント面 ベージュ `#e0ceaa`
- 文字色 Primary `#3c3c43`（純黒は使わない）、Secondary `#6d6d72`、Tertiary `#76767b`
- 罫線 `#d8d8d9` / `#ebebec`
- MUJI Red `#7f0019` は注釈番号や小さな強調のみ（広い面積に使わない）
- フォント: `"Helvetica Neue", Arial, "Noto Sans JP", "Hiragino Kaku Gothic ProN", Meiryo, sans-serif`
- 本文 16px / 行間 1.6。見出しも行間 1.6 を保つ
- 余白スケール: 4, 8, 12, 16, 20, 24, 32, 40, 48, 56, 64, 96 px のみを使う
- 本文の最大幅 720px（`--measure`）、地図のみ 1056px（`--measure-wide`）
- 角丸はボタンの 4px のみ。カードは角丸なし・影なし
- 過度なグラデーション・影・アニメーションを使わない。`palt` を使わない

---

## 9. ディレクトリ構成とデプロイ

```
/
├── public/                 ← Vercel の出力ディレクトリ
│   ├── index.html
│   ├── assets/css/style.css
│   ├── assets/js/app.js
│   ├── assets/img/proj-*.svg   ← scripts/build-thumbs.mjs が生成
│   ├── assets/vendor/*.js
│   └── data/{countries-110m.json, timeline.json, sources.json}
├── .github/workflows/pages.yml
├── package.json
├── SPEC.md
└── README.md
```

- ビルド工程なし（静的ファイルをそのまま配信）
- 本番デプロイ: GitHub Pages。`main` への push で `public/` をそのまま公開する
  （<https://mezum.github.io/eq-earth-site/>。アカウントの独自ドメイン
  <http://mezum.one/eq-earth-site/> に転送される）。プロジェクトページの配下に置くため、
  ページ内の参照はすべて相対パスにする
- ローカル開発プレビュー: `npm run preview`（依存関係なしの静的サーバ）
- 年表の外形図の再生成: `npm run build:thumbs`
